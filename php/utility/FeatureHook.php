<?php
declare(strict_types=1);

// Isports SDK utility: feature_hook

class IsportsFeatureHook
{
    public static function call(IsportsContext $ctx, string $name): void
    {
        if (!$ctx->client) {
            return;
        }
        $features = $ctx->client->features ?? null;
        if (!$features) {
            return;
        }
        foreach ($features as $f) {
            if (method_exists($f, $name)) {
                $f->$name($ctx);
            }
        }
    }
}
